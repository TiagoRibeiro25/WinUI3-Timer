<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinUI3_Timer.Pages.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WinUI3_Timer.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Unloaded="OnUnloaded"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </Page.Resources>

    <Grid Padding="32" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel HorizontalAlignment="Center" Spacing="24">
            <TextBlock x:Name="TimerNameBlock"
                       Text="{x:Bind ViewModel.SelectedTimerName, Mode=OneWay}"
                       Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" />

            <TextBlock x:Name="TimeDisplay"
                       Text="{x:Bind ViewModel.CurrentTime, Mode=OneWay}"
                       FontSize="96" FontWeight="SemiLight"
                       HorizontalAlignment="Center" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="8">
                <Button x:Name="StartButton" Content="Start" Click="OnStartClick"
                        Style="{StaticResource AccentButtonStyle}"
                        Visibility="{x:Bind ViewModel.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVisibility}, ConverterParameter=Invert}" />
                <Button x:Name="StopButton" Content="Stop" Click="OnStopClick"
                        Visibility="{x:Bind ViewModel.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />
                <Button Content="Reset" Click="OnResetClick" />
            </StackPanel>

            <TextBlock Text="No timer selected" Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center"
                       Visibility="{x:Bind ViewModel.HasSelectedTimer, Mode=OneWay, Converter={StaticResource BoolToVisibility}, ConverterParameter=Invert}" />
        </StackPanel>
    </Grid>
</Page>
